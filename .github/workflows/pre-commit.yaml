name: pre-commit
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  pre-commit:
    uses: cybcon/github_workflows/.github/workflows/pre-commit.yaml@bugfix/delta_branch_process
    with:
      full_precommit: false
